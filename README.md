# Konecta-POC
Konecta-POC is a frontend prototype showcasing an interactive Task Manager SPA built in Angular and React. It demonstrates state management, responsive UI, component structure, controlled forms, and full API-based CRUD operations, using a simple Node.js backend only for data persistence.
